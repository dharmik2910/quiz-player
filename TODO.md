- [ ] Inspect current completion + navigation flow (QuizPlayer -> onComplete -> ResultScreen -> back to quizzes)
- [x] Persist final quiz snapshot to localStorage on completion (mark completed + set last question index)

- [ ] Ensure snapshot persistence occurs both on timeout-complete and manual finish
- [ ] Test by completing quiz, going to quizzes page, then reopening same quiz and verifying it restores last state

